difficulty = input("Difficulty? ")
number = input("Problem Number? ")
name = input("Problem Name? ")

def commiter(difficulty, number, name):
    
    #open folder leetCodeSolved
    #open folder [difficulty]
    #add file [problemNumber_problemNameParsed(i.e Two_Sum)]
    #run 
        #git add .
        #git commit -m "Create [fileName]"
        #git push origin main

    return
